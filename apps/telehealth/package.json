{
  "name": "@turbo-demo/telehealth",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "checkly:test": "pnpm exec checkly test",
    "checkly:deploy": "pnpm exec checkly deploy --force"
  },
  "dependencies": {
    "@turbo-demo/shared-helpers": "workspace:*"
  },
  "devDependencies": {
    "@playwright/test": "^1.50.0",
    "checkly": "^7.4.0",
    "typescript": "^5.7.0"
  }
}
